name:Python Test
on:[Push]
jobs:
	test:
		runs-on:ubuntu-latest
	steps:
		-name:checkout code
		uses:actions/checkout@V4
		
		-name:set up python
		uses:actions/set up-python@V5

		with:
			Python-version:"3.10"
		
		-name:Run test
		run:Python print.py.